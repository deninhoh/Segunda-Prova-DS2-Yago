@model IEnumerable<ProvaDS2Denner.Models.PecaDeRoupa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoDeRoupa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoDeRoupa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PecaDeRoupaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PecaDeRoupaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PecaDeRoupaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
